PABLO:
    implementar:
    check_login(username, password):
        True si la contrasenya és correcta, false si no

    check_username(username):
        True si l'usuari existeix a la database, false si no (i per tant està disponible)

    create_user(username, password):
        Crea un nou usuari amb les dades corresponents

    totes les funcions estan al app.py, al final de tot
MIQUEL:
    implementar: 
        preparar per a que el pablo pugui fer la funció fetch_info (pensar el sistema de information flow)
        botó de logout
    web: 
        bot config: 
            sliders per a les tres variables (vida, escut, atac)
            botó per a enviar les dades
            informació sobre cada cosa?
        player info:
            mostrar les dades del jugador ()